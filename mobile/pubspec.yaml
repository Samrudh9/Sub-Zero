name: sub_zero
description: AI-Powered Subscription Assassin
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_card_swiper: ^6.0.0
  http: ^1.1.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
